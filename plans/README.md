# Implementation Plans

Generated by the improve skill on 2026-06-14. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Route shell imports through public feature entrypoints | P1 | S | - | TODO |
| 002 | Require explicit enablement for imported JavaScript extensions | P1 | M | - | TODO |
| 003 | Add shadcn configuration for the real De-Koi Tailwind entrypoint | P2 | S | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale: finding fixed independently or approach abandoned)

## Dependency Notes

- 001 has no dependency and should land first because `pnpm check:architecture` is a documented repo gate and currently fails.
- 002 is independent of 001. It touches the settings/extension surface and should be reviewed with a security lens.
- 003 is independent of 001 and 002. It is a DX/tooling cleanup for future shadcn CLI use.

## Findings Considered And Rejected

- Development-only scene-analysis logging in `src/features/modes/game/components/GameSurface.tsx`: rejected as a production finding because the `console.log` is guarded by `import.meta.env.DEV`.
- Dependency audit advisories for `tmp` and `esbuild`: not planned in this batch because package/dependency declaration changes require explicit approval in De-Koi workflow. Evidence remains `pnpm audit --audit-level high`, `package.json:63`, and `pnpm-lock.yaml:1960,3362,3366`.
- Rust dependency advisory audit: not planned because `cargo audit` is not installed in the current environment. A future dependency plan should either install/use the repo-approved Rust advisory tool or document why `cargo deny` is the intended substitute.
