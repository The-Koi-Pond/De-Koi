interface:
  display_name: "Frontend Design"
  short_description: "De-Koi UI concept and build lens"
  default_prompt: "Use $frontend-design to design and build this De-Koi UI surface, then use Impeccable for polish if needed."
