# Codex Skill Follow-ups

- Add a Windows-equivalent flow to the Playwright skill wrapper. On Windows, prefer `npx.cmd --package @playwright/cli playwright-cli` for the skill's interactive snapshot/click/fill commands, and fall back to repo-local `node_modules/.bin/playwright.CMD` for standard Playwright CLI commands.
- While working in De-Koi, replace non-attribution mentions of "Marinara Engine" or "Marinara" with "De-Koi"; keep historical attribution, fork/disclaimer, and compatibility references intact.
