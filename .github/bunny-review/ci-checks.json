{
  "expected_checks": [
    { "name": "Deterministic Repository Gates", "required": "always" },
    { "name": "Rust Capability Layer", "required": "always" },
    { "name": "Browser Smoke and Performance", "required": "always" }
  ]
}
